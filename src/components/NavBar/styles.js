import styled from 'styled-components'
import { white } from '../../styles/colours'

export const NavContainer = styled.div`
  position: fixed;
  top: 0;
  width: 100%;
  overflow: hidden;
  box-shadow: 0 0 20px 6px rgba(233, 233, 233, 0.55);
  background: ${white};
  padding-bottom: 20px;
`
